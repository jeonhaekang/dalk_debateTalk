import React from "react";
import Button from "../../elements/Button";
import Grid from "../../elements/Grid";

const SearchCard = (props) => {
    return (
        <Grid display="flex">
            <Grid>토론 검색(버튼없이 검색만 해도 바로 나올 수 있게)</Grid>
        </Grid>
    )
};

export default SearchCard;