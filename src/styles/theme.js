const fontSizes = {
  headline: 32,
  subtitle: 24,
  bigBody: 16,
  smallBody: 14,
};

const fontWeight = {
  semiBold: 600,
  medium: 500,
  regular: 400,
  light: 300,
};

const color = {};

const theme = {
  fontSizes,
  fontWeight,
  color,
};

export default theme;
